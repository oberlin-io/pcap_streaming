#
#
# regex field for tcp dump triple verbose
# make json
fields:
- name:   time
  regex:  (\d{2}:\d{2}:\d{2}.\d+)\s
- name:   source
  regex:  \s([\d\.]+)\s>
- name:   destination
  regex:  >\s([\d\.]+):
